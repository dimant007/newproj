entities:
  Article:
    fields:
      name: {type: string}
      url: {type: string_url}
      image: {type: string_image}
      short_description: {type: string}
      full_description: {type: string}